<%= form_with(model: annee, local: true) do |form| %>
  <% if annee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(annee.errors.count, "error") %> prohibited this annee from being saved:</h2>

      <ul>
        <% annee.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Libelle %>
    <%= form.text_field :libelle, :class => 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :Etat %>
    <%= form.check_box :etat %>
  </div>

  <div class="actions">
    <%= form.submit "VALIDER", :class => 'btn btn-primary' %>
  </div>
<% end %>