<%= form_with(model: heure, local: true) do |form| %>
  <% if heure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(heure.errors.count, "error") %> prohibited this heure from being saved:</h2>

      <ul>
        <% heure.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :libelle %>
    <%= form.text_field :libelle %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
